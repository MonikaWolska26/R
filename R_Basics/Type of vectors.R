#Write a R program to create three vectors numeric data, character data 
#and logical data. Display the content of the vectors and their type.

vec1 <- c(1, 2, 3)
vec2 <- c('a', 'b', 'c')
vec3 <- c(T, F, T)

print(vec1)
print(typeof(vec1))

print(vec2)
print(typeof(vec2))

print(vec3)
print(typeof(vec3))